# Gitleaks config for my-pi-dotfiles
title = "my-pi-dotfiles"

[allowlist]
description = "Placeholder auth example and docs"
paths = [
  '''auth/auth\.json\.example$''',
  '''README\.md$''',
]
regexes = [
  '''REPLACE_ME''',
]
